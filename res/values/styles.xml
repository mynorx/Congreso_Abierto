<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light">
        <!--Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here. -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:windowTitleBackgroundStyle">@style/WindowTitleBackground</item>
    </style>
    
    <!-- Creamos nuevos estilos -->
    
    <!-- Estilo para los items del menu -->
    <style name="estilo_navigation_drawer">
        <item name="android:textColor">#FFFFFF</item>
    </style>
    
    <!-- Estilo para ListView 1 -->
    <style name="estilo_listview_1">
        <item name="android:background">@drawable/shape_list</item>
        <item name="android:textColor">@drawable/txt_select</item>
    </style>
    
    <!-- Estilo ActionBar -->
    <style name="WindowTitleBackground">     
        <item name="android:background">@drawable/barra</item>
    </style>
</resources>
